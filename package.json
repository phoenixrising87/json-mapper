{
  "name": "json-mapper",
  "version": "1.0.0",
  "description": "map json objects to typed objects",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "test": "jasmine-ts src/**/*.spec.ts",
    "build": "rm -rf ./dist && tsc"
  },
  "author": "Manuel Heim",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4",
    "reflect-metadata": "^0.1.10"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.2",
    "@types/lodash": "^4.14.85",
    "jasmine": "^2.8.0",
    "jasmine-core": "^2.8.0",
    "jasmine-ts": "^0.2.1",
    "typescript": "^2.6.1"
  }
}
